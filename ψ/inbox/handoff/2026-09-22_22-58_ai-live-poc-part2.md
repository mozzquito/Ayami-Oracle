# Handoff: AI Live POC — Ollama fix, browser avatar, commit

📡 Session: 6c61b712 | ayami-oracle | ~30m (continuation of be7b708f)
**Date**: 2026-09-22 22:58
**Context**: 165k (past 150k warn threshold — this is why we're handing off)

## Context
**Oracle**: Ayami Oracle (หญิง) | **Human**: มอส (ชาย)
**Mode**: Fast | **Memory**: Auto

## What We Did

Picked up from `2026-09-22_22-25_ai-live-poc.md` (previous session's handoff)
and cleared 4 of its 5 pending items:

1. **Ollama fix** — pulled `qwen2.5:3b` (1.9GB, was `qwen2.5:7b` timing out
   60s+ on this 8GB M1). First cold-load call took ~60s (41s load + 16s
   generation), which blew past `responder.py`'s old 15s timeout and
   silently fell back to the canned reply — bumped timeout 15s→30s
   (`src/ai_live_poc/responder.py:21`). Re-ran the orchestrator twice after:
   confirmed real (non-fallback) LLM replies both times.

2. **Built the browser/avatar side** (was "designed, not implemented"):
   - `src/ai_live_poc/server.py` — FastAPI app, runs `orchestrator.run()` as
     a background task, fans events out over SSE (`GET /events`), serves
     `public/index.html` at `/` and `audio/*.mp3` via `StaticFiles`.
   - `public/index.html` — single-page 2D SVG avatar (no external
     libs/CDN). Mouth openness driven by the TTS audio's own amplitude via
     Web Audio API `AnalyserNode` (RMS of time-domain buffer, sampled per
     animation frame) — the audio-only-avatar approach the zcode×2+agy
     consult recommended instead of real-time neural lip-sync. Start button
     unlocks `AudioContext` on click (browser autoplay policy). Live chat
     feed shows viewer comments + replies + throttled/blocked turns so the
     safety gate's effect is visible.
   - Verified via curl (SSE emits real events, `.mp3` served 200 not 404),
     then via **actual browser click-test** — Chrome extension wasn't
     connected at first, used computer-use (read-only tier) to confirm the
     page renders correctly, then มอส clicked "▶ เริ่มไลฟ์" himself and
     confirmed mouth animation + audio playback both work.

3. **OBS → MediaMTX RTMP wiring** — checked, neither is installed on this
   machine (`brew list` came up empty for both). Asked มอส; his call:
   **skip for now**, revisit when ready to install both. Not started.

4. **Re-verify Shopee AI-host policy page directly** — attempted 3 ways,
   all failed to render the actual page content: WebFetch (twice now, same
   empty "Seller Education Hub" header), archive.org wayback snapshot
   (blocked — can't fetch that domain at all), and the Chrome extension
   actually loading the live page (hung with "script injection timed out"
   3x in a row, no content ever appeared — cause unclear: bot-detection,
   region-gate, login-wall, or genuinely heavy page are all possible; did
   not attempt to force past it). Cross-confirmed the same policy facts via
   an independent `WebSearch` instead (matches the original research pass:
   approval form required, no reusing one AI host across accounts, no
   pre-recorded-as-live, warning→deboost→terminate→temp-ban→permanent-ban
   escalation) — two independent searches agreeing is decent corroboration
   but still not primary-source full text. Documented the attempt in
   `ψ/active/context/2026-09-22_ai-live-streaming-tiktok-shopee.md` (§2,
   "Re-verify attempt" note) and in the outbox pending file. **This item
   stays genuinely open** if a real compliance decision ever depends on it.

5. **Committed** `ψ/lab/ai-live-poc/` — commit `858a8656` on `lab/jev-gate`,
   15 files (all the pipeline code + README + the handoff + pending list).
   Deliberately left out of the commit: `ψ/inbox/focus-agent-*.md`,
   `ψ/lab/grok-live-watch/logs/poll.log`, and the untracked
   `ψ/memory/learnings/2026-09-22_typesafe-ai-skills-plugin.md` — none of
   these were touched by this session's work (see Cleanup Checklist below,
   carried forward unchanged from the previous handoff).

## Pending

- [ ] Wire OBS Browser Source → MediaMTX local RTMP sink — not started,
      needs `brew install --cask obs` + `brew install mediamtx` first
      (มอส explicitly deferred this)
- [ ] Direct-fetch verification of Shopee's AI-host policy page — blocked by
      tooling (JS-heavy page hangs every render attempt tried so far);
      cross-confirmed via WebSearch instead, see research doc for detail
- [ ] `responder.py`'s 30s timeout still won't cover a *cold* model load
      (41s observed) if Ollama evicts the model between calls — a
      long-idle live session would hit fallback again on the first reply
      after an idle gap. Keeping the model warm (periodic ping, or
      `ollama run <model> --keepalive -1`) is the likely fix, not attempted.
- [ ] One reply quality issue noted earlier in the session: qwen2.5:3b
      occasionally produces slightly odd/mixed-language Thai — not
      investigated further, may just be model-size tradeoff.

## Cleanup Checklist (unrelated pre-existing state, carried forward again)

- [ ] Uncommitted: `ψ/inbox/focus-agent-agy.md`, `ψ/inbox/focus-agent-main.md`,
      `ψ/lab/grok-live-watch/logs/poll.log` — still modified, still not from
      any ai-live-poc session's work
- [ ] Untracked: `ψ/memory/learnings/2026-09-22_typesafe-ai-skills-plugin.md`
      — still not filed
- [ ] Stale worktree branches (unchanged from last time, still not
      investigated): `worktree-calm-swinging-castle`,
      `worktree-commit-remaining-outside-lab`,
      `worktree-immutable-zooming-yeti`, `worktree-lab-commit-triage`,
      plus `fix/token-reduction`, `lab/sendgrid-relay-migration`,
      `lab/statusline-v2`, `learn/zcode-silent-upload`

## Key Files

- `ψ/lab/ai-live-poc/README.md` — full project status, now up to date
- `ψ/lab/ai-live-poc/src/ai_live_poc/server.py` — new, FastAPI/SSE
- `ψ/lab/ai-live-poc/public/index.html` — new, browser avatar page
- `ψ/lab/ai-live-poc/src/ai_live_poc/responder.py` — timeout bump (line 21)
- `ψ/active/context/2026-09-22_ai-live-streaming-tiktok-shopee.md` — research,
  now with the re-verify attempt logged
- `ψ/outbox/2026-09-22_pending.md` — pending items, now with statuses
- Commit: `858a8656` on branch `lab/jev-gate` (not pushed)

## Next Session: Pick Your Path

| Option | Command | What It Does |
|--------|---------|--------------|
| **Continue this POC** | `/recap` then decide on OBS/MediaMTX or keep-warm fix | Pick up where we left off |
| **Clean up first** | See cleanup checklist above, then `/recap` | Handle stale branches/uncommitted files, then continue |
| **Fresh start** | `/recap --quick` | Minimal context, start something new |
