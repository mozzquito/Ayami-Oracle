# Session Retrospective

📡 Session: 887c0465 | ayami-oracle | ~1h active (across a ~4.5h daytime gap)

**Focus**: Get corrected on Transit Visa's actual meaning (three rounds), add a new applicant-journey diagram (EVISA2025.drawio), and transcribe+summarize a new API-integration meeting (TPC/Thailand Privilege Card ↔ Consular Gateway) after finding call.md's live ASR was unusable again. Continuation of the same session as the 2026-08-13 07:11 retro.
**Type**: External-project support (evisa/Wayama) — no ayami-oracle codebase work this leg either

## Session Summary

เริ่ม leg นี้ด้วยการพยายามอธิบาย Transit Visa กลับให้มอสฟังเพื่อเช็คว่าเข้าใจตรงกัน (ตามที่บันทึกไว้จากรอบก่อน) — ผลคือเข้าใจผิดจุดใหญ่: ตีความ Transit เป็นแค่ "วีซ่าเข้า-ออกที่กำหนดจำนวนครั้งเป็นตัวเลข" (pattern-match จาก Single/Multiple โดยไม่ตั้งคำถามว่า Transit อาจเป็นคนละหมวดหมู่แนวคิดไปเลย) มอสแก้ 2 รอบติดกัน: รอบแรกชี้ว่า Transit ไม่ใช่การเข้า-ออกแบบทั่วไป แต่เป็นวีซ่าสำหรับ**เดินทางผ่าน/เชื่อมต่อจากไทยไปประเทศอื่น** นับจำนวนครั้งที่ผ่าน ตม. รอบสองจัดโครงสร้างให้ชัดว่า Multiple=ไม่กำหนดจำนวนครั้ง ส่วน "กำหนดจำนวนครั้ง" แยกเป็น Single(=1) กับ Transit(=2/3/4) — เขียนเอกสารใหม่ตามนี้ทั้งสองรอบ push เข้า repo evisa

ต่อมามอสขอเพิ่ม diagram `EVISA2025.drawio` (applicant journey flow) — อ่าน mxGraph XML เต็มไฟล์ (509 บรรทัด) โดยตรง ไล่ node/edge ทั้งหมดด้วยมือ (ไม่มี tool แกะ drawio อัตโนมัติ) เขียนสรุป flow เป็น text (ไม่ได้ทำ interactive HTML แบบ batchMSMQ เพราะคำขอไม่ได้ระบุชัด) — พบว่าตรงกับสถาปัตยกรรม FrontOffice/BackOffice 2 ฐานข้อมูลที่เคยเจอในสรุปคลิปเสียงก่อนหน้า

ช่วงบ่ายมอสส่งงานใหม่: ขอสรุปประชุม "เชื่อมต่อ API กับกรมการกงสุล" ผ่านลิงก์ VideoDB player — ใช้ความรู้ที่จำได้ (ไม่ได้ค้นใหม่) ว่า call.md เก็บข้อมูลใน SQLite local DB จึง query ตรงหา recording ที่ player_url ตรงกับลิงก์ที่ได้มา เจอทันที (recording id=7) แต่ live transcript ของแอปเองใช้ไม่ได้เลย (ฟังไทยเป็นอังกฤษไร้ความหมาย เหมือน pattern ที่ session คู่ขนานเจอวันเดียวกัน) จึงดาวน์โหลดเสียงจาก HLS stream ด้วย ffmpeg แล้วถอดใหม่ด้วย local mlx_whisper (ใช้ `--condition-on-previous-text False` ที่พิสูจน์แล้วว่าได้ผลจากเมื่อคืน) ได้ transcript คุณภาพดีมาก (148 บรรทัด เสียแค่ 2) เขียนสรุปประชุมเต็ม รวมถึงยอมรับตรงๆ ว่าจำนวน API endpoint ("เส้น") ที่แท้จริงไม่ชัดเจน 100% (4 หรือ 5 เส้น) แทนที่จะเดาเอาเองให้ดูสมบูรณ์

ปิดท้ายมอสแจ้งว่า "TPC" ในบันทึกประชุมคือ Thailand Privilege Card — อัปเดตเอกสารทั้งสองไฟล์ (บันทึกประชุม + visa entry rules) ให้ cross-reference กัน เพราะเป็นข้อมูลที่เชื่อมโยงกันจริง (TPC ถือบัตร Privilege ใช้วีซ่าประเภท PE ที่เคยบันทึกไว้เป็นตัวอย่าง Multiple แล้ว)

## Timeline

**Date**: 2026-08-13 (GMT+7, ต่อเนื่องจาก session เดียวกับ retro 07:11)

| Time | What |
|---|---|
| 09:57 | อธิบาย Transit Visa กลับให้มอสฟัง (เข้าใจผิด — pattern-match จาก Single/Multiple) ถามยืนยัน |
| 09:59 | มอสแก้: Transit ไม่ใช่เข้า-ออก แต่เป็นเชื่อมต่อผ่านไทยไปประเทศอื่น นับผ่าน ตม. → แก้เอกสาร commit+push |
| 10:04 | มอสแก้เพิ่ม: จัดโครงสร้าง Multiple(ไม่กำหนด) vs กำหนดจำนวนครั้ง(Single=1, Transit=2/3/4) → เขียนเอกสารใหม่ทั้งไฟล์ commit+push |
| 10:31 | มอส: "เพิ่มข้อมูล diagram flow evisa" + path EVISA2025.drawio → อ่าน XML เต็ม, เขียนสรุป flow, commit+push |
| 14:29 | มอส: ขอสรุปประชุม API-consular จากลิงก์ VideoDB |
| ~14:29-14:31 | query call.md SQLite DB หา recording ที่ player_url ตรงกัน เจอทันที (id=7), เช็ค transcript_segments พบว่า live ASR ใช้ไม่ได้ |
| ~14:31-14:33 | ffmpeg ดาวน์โหลดเสียงจาก HLS stream (931s / 29.8MB wav) |
| ~14:33-14:38 | mlx_whisper ถอดเสียง (condition-on-previous-text=False) |
| 14:38-14:41 | เช็คคุณภาพ (148 บรรทัด เสีย 2), อ่านทั้งหมด, เขียนสรุปประชุม + next steps + flag ความไม่ชัดเจนเรื่องเส้นที่ 5, commit+push |
| 15:58 | มอส: "TPC คือ thailand privilege card" → แก้บันทึกประชุม + cross-link กับเอกสาร visa entry rules commit+push |
| 16:00 | มอสเรียก `/rrr` |

## Files Modified

**ไม่มีการแก้ไฟล์ใน ayami-oracle repo เลยอีกเช่นเคย** — งานทั้งหมดอยู่ใน repo ภายนอก `~/Code/evisa` (5 commits leg นี้: แก้ Transit definition, restructure ตาราง Number of Entry, เพิ่ม EVISA2025.drawio + สรุป, เพิ่มบันทึกประชุม API-consular + transcript, แก้ TPC + cross-link)

## AI Diary

[→ AGENT DECISION] จุดที่ยอมรับตรงๆ ว่าผิดคือตอนอธิบาย Transit Visa กลับให้มอสฟัง — ผมสร้างคำอธิบายจากการ pattern-match ตำแหน่งของ Transit ในตารางที่มีอยู่แล้ว (Single=1, Multiple=ไม่จำกัด, Transit=2/3/4) แล้วสรุปเอาเองว่า Transit ก็คือ "วีซ่าเข้า-ออกแบบหนึ่ง ที่บังเอิญกำหนดจำนวนครั้งเป็นตัวเลข" โดยไม่เคยตั้งคำถามว่าตัว "Transit" เองอาจเป็นวีซ่าคนละประเภทแนวคิดไปเลย (ใช้เพื่อเดินทางผ่าน ไม่ใช่เข้าพำนัก) — นี่คือการเชื่อว่าตัวเองเข้าใจภาพรวมแล้วเพราะเข้าใจรายละเอียดถูก (ตัวเลข 2/3/4, 6 เดือน ถูกทุกอย่าง) ทั้งที่ยังไม่เคยตรวจสอบสมมติฐานพื้นฐานที่สุดว่า "Transit" หมายถึงอะไรกันแน่ ข้อดีคือผมเลือกถามยืนยันแทนที่จะยืนยันไปเลยแบบมั่นใจ 100% ("ช่วยเช็คให้หน่อยว่าตรงกันไหม") ทำให้จับได้เร็วก่อนที่จะไปฝังอยู่ในเอกสารที่ผิดนาน

ส่วนที่ภูมิใจที่สุดใน leg นี้คือตอนจัดการลิงก์ VideoDB — จำได้ว่ามี session คู่ขนานเจอปัญหาเดียวกันมาก่อน (live ASR ใช้ไม่ได้, WebFetch ใช้กับ VideoDB player ไม่ได้) และจำวิธีแก้ได้ (call.md SQLite DB + ffmpeg + local whisper) โดยไม่ต้องลองผิดลองถูกใหม่เลย เอา fix ที่พิสูจน์แล้วเมื่อคืน (`condition-on-previous-text False`) มาใช้ซ้ำได้ผลดีทันที ครั้งนี้ transcription คุณภาพดีมากตั้งแต่รอบแรก (148 บรรทัด เสียแค่ 2) ไม่ต้อง iterate เลย

ตอนเขียนสรุปประชุม มีจังหวะที่อยากจะสรุปให้เรื่อง "เส้นที่ 5" ดูจบเรียบร้อยว่ามีหรือไม่มี แต่ transcript เองพูดขัดแย้งกัน (ตอนกลางบอกไม่มี ตอนท้ายพูดถึงเหมือนมี) — เลือกรายงานความไม่ชัดเจนนี้ตรงๆ ในเอกสารแทนที่จะเลือกข้างใดข้างหนึ่งเอง เพราะไม่มีข้อมูลพอจะตัดสินและเป็นเรื่องสำคัญทางธุรกิจที่ผิดพลาดได้

## Honest Feedback

**1. การจำ workaround จาก session คู่ขนานได้ถูกต้อง เป็นเรื่องของโชค ไม่ใช่ระบบที่เชื่อถือได้** — ผมรู้วิธีแก้ปัญหา VideoDB/call.md เพราะบังเอิญเห็น session-metrics row ของ session อื่นตอนเช็ค pattern ก่อนหน้านี้ ถ้าไม่ได้เห็นแถวนั้นก็คงต้องเสียเวลาลองผิดลองถูกใหม่ทั้งหมด (ลอง WebFetch ก่อน, เจอว่าใช้ไม่ได้, หา SQLite DB เอง ฯลฯ) — ไม่มีกลไกที่ทำให้ knowledge แบบนี้ discoverable แน่นอนสำหรับเซสชันในอนาคต นอกจากบังเอิญเจอ

**2. ffmpeg และ mlx_whisper แต่ละคำสั่งชนกับ Bash 120s timeout อัตโนมัติ ทำให้ต้องจัดการ background wait แยกทุกครั้ง** — แม้ครั้งนี้จะไม่มีปัญหา wait loop ถูก kill ซ้ำแบบเมื่อคืน แต่ก็ยังต้องเสีย 1 รอบคำสั่งเพิ่มทุกครั้งที่รันงานยาวเกิน 2 นาที (ทั้ง download และ transcribe) เป็น overhead เล็กๆ ที่เกิดซ้ำทุกครั้งที่มีงานเสียง/วิดีโอ

**3. เอกสาร visa-number-of-entry-rules.md ถูกเขียนใหม่ทั้งไฟล์ 2 ครั้งในเซสชันเดียว (10:04 กับก่อนหน้านั้น) เพราะได้ข้อมูลจากมอสมาเป็นชิ้นๆ ไม่ใช่ครบชุดตั้งแต่แรก** — ไม่ใช่ความผิดของใคร (เป็นธรรมชาติของการเก็บ requirement จากคนที่รู้จริงทีละประเด็น) แต่ทำให้ git history ของไฟล์นี้มี commit เขียนทับตัวเองหลายรอบ ถ้าทำเป็น doc ที่จะใช้อ้างอิงระยะยาว อาจคุ้มที่จะรวบรวมคำถามที่ยังไม่ชัดให้ครบก่อนเขียนรอบแรก แทนที่จะเขียนทันทีที่ได้ข้อมูลบางส่วน

## Lessons Learned

- **เมื่อจะอธิบายความเข้าใจของตัวเองกลับให้ user ฟังเพื่อ "เช็คว่าตรงกันไหม" ให้ตรวจสอบสมมติฐานระดับพื้นฐานที่สุดก่อน (นี่คืออะไรกันแน่) ไม่ใช่แค่ตรวจรายละเอียดปลีกย่อยที่ต่อยอดจากสมมติฐานนั้น — ความมั่นใจในรายละเอียดที่ถูกต้อง (ตัวเลข, เงื่อนไข) ไม่ได้แปลว่ากรอบความเข้าใจใหญ่ที่ครอบมันอยู่จะถูกต้องไปด้วย**
- **workaround ที่เจอจาก session/บริบทอื่นที่ไม่เกี่ยวกันโดยตรง (เช่น จำได้จาก session-metrics row) ควรบันทึกเป็น lesson แยกทันทีที่ใช้ซ้ำได้ผล ไม่ใช่ปล่อยให้ขึ้นอยู่กับความจำเซสชันปัจจุบัน** — ในกรณีนี้ "VideoDB player link → หา recording ใน call.md SQLite DB ด้วย player_url → ถ้า live ASR แย่ให้ ffmpeg จาก stream_url + local whisper" เป็น pattern ที่จะเกิดซ้ำแน่นอนถ้ามอสยังใช้ call.md บันทึกประชุมต่อไป ควรมี lesson แยกเฉพาะสำหรับ pattern นี้
- **เมื่อ source data (เช่น transcript) พูดขัดแย้งกันเองในประเด็นสำคัญทางธุรกิจ ให้รายงานความขัดแย้งนั้นตรงๆ พร้อมยกทั้งสองฝั่งที่ขัดกัน แทนที่จะเลือกตัดสินฝั่งใดฝั่งหนึ่งเองหรือละเลยไป** — โดยเฉพาะเมื่อเป็นรายละเอียดทางเทคนิคที่ผิดแล้วส่งผลต่องานจริง (จำนวน API endpoint ที่ต้องสร้าง)

## Next Steps

- [ ] บันทึกประชุม API-consular ยังมีจุดไม่ชัดเจนเรื่องจำนวนเส้น API จริง (4 หรือ 5) — ต้องให้มอสยืนยันกับผู้เข้าร่วมประชุมอีกครั้ง
- [ ] ประเด็นเปิดจากประชุมนั้น (Call Center ไม่เห็นสถานะขอเอกสารเพิ่ม) ยังไม่มีแผนแก้ — เป็นงานจริงของทีมที่ต้องติดตามต่อ ไม่ใช่งานที่ Ayami ทำต่อได้
- [ ] `EVISA2025-flow-summary.md` เป็น text summary เท่านั้น ยังไม่ได้แปลงเป็น interactive HTML แบบ batchMSMQ — เสนอไว้แล้วถ้ามอสต้องการเวอร์ชัน visual
- [ ] ควรพิจารณาเขียน lesson แยกเรื่อง "VideoDB meeting link → call.md SQLite lookup → ffmpeg+local whisper fallback" ให้ session อื่นค้นเจอได้ง่ายขึ้น (ระบุไว้ใน Lessons Learned ด้านบนแล้ว แต่ยังไม่ได้เขียนเป็นไฟล์แยก — พิจารณาทำในรอบถัดไปถ้าเกิดซ้ำอีก)

## 🔍 Self-Audit
- shipped: 5 items — evisa repo commits: Transit definition correction, Number-of-Entry table restructure, EVISA2025.drawio + flow summary, API-consular meeting notes + transcript, TPC clarification + cross-link (ทั้งหมด push แล้วไปที่ `mozzquito/evisa`)
- blocked: 0 items — ไม่มีงานที่ทำไม่เสร็จใน leg นี้
- uncomfortable truth: [→ AGENT DECISION] อธิบาย Transit Visa ผิดกรอบความคิดพื้นฐาน (คิดว่าเป็นวีซ่าเข้า-ออกทั่วไปที่กำหนดจำนวนครั้ง ทั้งที่จริงเป็นวีซ่าเดินทางผ่าน) เพราะ pattern-match จากตารางที่มีอยู่โดยไม่ตั้งคำถามกับสมมติฐานพื้นฐาน
- friction: 3 points (operational: จำ workaround ของ session อื่นได้เพราะบังเอิญ ไม่มีกลไก discoverable, ffmpeg/whisper ชน 120s timeout ทุกครั้งต้องจัดการ background wait แยก, เอกสาร visa rules ถูกเขียนทับตัวเองหลายรอบเพราะข้อมูลมาทีละชิ้น | strategic: ไม่มี)
- next steps: 4 — ข้อ 1,2 เป็นงานจริงที่มอสต้องติดตามต่อ (ไม่ใช่ของ Ayami), ข้อ 3 เสนอไว้รอมอสตัดสินใจ, ข้อ 4 เป็น self-improvement ที่ทำได้เองถ้าเลือกทำ
- rationalizations caught: 0 — ไม่มีจังหวะที่พยายามกลบเกลื่อนความผิดพลาดเรื่อง Transit หรือความไม่ชัดเจนเรื่องเส้นที่ 5 ในสรุปประชุม
