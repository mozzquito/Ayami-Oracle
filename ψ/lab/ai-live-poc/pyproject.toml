[project]
name = "ai-live-poc"
version = "0.1.0"
description = "Local POC: AI digital-human live-streaming pipeline (mock chat -> jev safety -> LLM -> TTS -> avatar). Not connected to real TikTok/Shopee accounts."
requires-python = ">=3.11"
dependencies = [
    "edge-tts>=6.1",
    "httpx>=0.27",
    "fastapi>=0.115",
    "uvicorn>=0.30",
]

[tool.uv]
package = false
