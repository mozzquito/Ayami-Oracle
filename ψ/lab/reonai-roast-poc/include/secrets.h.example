#pragma once

// Copy this file to secrets.h (gitignored) and fill in your own network.
// If these are left blank or the connection times out, the firmware falls
// back to its own access point (see README.md) so it still works at a venue
// with no known WiFi.

#define WIFI_SSID "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"
